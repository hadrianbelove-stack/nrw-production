name: "Daily NRW Update"

on:
  schedule:
    - cron: '0 9 * * *'  # 9 AM UTC daily
  workflow_dispatch:  # Manual trigger for testing

concurrency:
  group: nrw-automation
  cancel-in-progress: true

permissions:
  contents: write
  issues: write

jobs:
  daily-update:
    runs-on: ubuntu-latest
    timeout-minutes: 300  # 5 hours for full pipeline: intake + discovery (~12k titles) + enrichment (120 min cap)
    env:
      PYTHONUNBUFFERED: '1'  # Disable Python output buffering for real-time logs
      TMDB_API_KEY: ${{ secrets.TMDB_API_KEY }}
      OMDB_API_KEY: ${{ secrets.OMDB_API_KEY }}
      GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      MIN_PROVIDER_COVERAGE: ${{ secrets.MIN_PROVIDER_COVERAGE }}
      B2_KEY_ID: ${{ secrets.B2_KEY_ID }}
      B2_APPLICATION_KEY: ${{ secrets.B2_APPLICATION_KEY }}
      B2_BUCKET_NAME: ${{ secrets.B2_BUCKET_NAME }}
      # Playwright configuration
      NRW_USE_SHARED_PLAYWRIGHT: 'true'  # Use shared PlaywrightManager (now default behavior)

    steps:
      - name: Branch guard
        run: |
          EXPECTED_BRANCH="${OVERRIDE_EXPECTED_BRANCH:-main}"
          CURRENT_BRANCH="${GITHUB_REF#refs/heads/}"
          if [[ "$CURRENT_BRANCH" != "$EXPECTED_BRANCH" ]]; then
            echo "❌ Workflow running on unexpected branch: $CURRENT_BRANCH"
            echo "Expected: $EXPECTED_BRANCH"
            echo "Set OVERRIDE_EXPECTED_BRANCH env var to override this check"
            exit 1
          fi
          echo "✅ Branch guard passed: running on $CURRENT_BRANCH"

      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright browsers
        run: |
          python3 -m playwright install --with-deps chromium

      - name: Run optional validation tests
        if: env.NRW_RUN_OPTIONAL_TESTS == 'true'
        run: |
          echo "🧪 Running optional contract and newsletter validation tests..."
          python3 -m pytest tests/test_discovery_contract.py -v
          python3 -m pytest tests/test_newsletter_mapping.py -v
        continue-on-error: true

      - name: Configure Git
        run: |
          git config user.name "NRW Bot"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      # Hard guard: the hand-curated capsule bank must never be CI-managed.
      # If it ever becomes git-tracked, CI could overwrite the user's curated
      # capsules — fail loudly instead.
      - name: Guard hand-curated capsule bank
        run: |
          if git ls-files --error-unmatch cache/approved_capsules.json >/dev/null 2>&1; then
            echo "::error::cache/approved_capsules.json is tracked — CI must never manage the curated capsule bank"
            exit 1
          fi
          echo "✅ approved_capsules.json is not tracked (correct)"

      - name: Run daily pipeline
        run: python3 daily_orchestrator.py | tee .local_run.log

      # --- Curation pre-compute (moved off the local Mac) -------------------
      # These two steps do the slow Gemini/Playwright work that used to run on
      # the laptop overnight. They are NON-CRITICAL: a scraping hiccup must
      # never block the data.json publish, so both use continue-on-error.
      # Results are handed back to the Mac via the "curation-caches" artifact
      # (see the upload step below) — they are NOT committed to git.
      - name: Pre-scrape pull quotes for new arrivals
        if: always()
        continue-on-error: true
        run: |
          python3 scripts/batch_pull_quotes.py || echo "Pull-quote pre-scrape failed, continuing..."

      - name: Pre-generate capsules for new arrivals
        if: always()
        continue-on-error: true
        run: |
          python3 scripts/write_capsule.py --batch --days 7 --variants 3 --skip-verify \
            || echo "Capsule pre-gen failed, continuing..."

      - name: Restore YouTube credentials
        env:
          YOUTUBE_TOKEN: ${{ secrets.YOUTUBE_TOKEN }}
          YOUTUBE_CLIENT_SECRET: ${{ secrets.YOUTUBE_CLIENT_SECRET }}
        run: |
          mkdir -p youtube_credentials
          if [ -n "$YOUTUBE_CLIENT_SECRET" ] && [ -n "$YOUTUBE_TOKEN" ]; then
            echo "$YOUTUBE_CLIENT_SECRET" | base64 -d > youtube_credentials/client_secret.json
            echo "$YOUTUBE_TOKEN" | base64 -d > youtube_credentials/token.pickle
            echo "YouTube credentials restored"
          else
            echo "YouTube credentials not configured, skipping playlist generation"
          fi

      - name: Update weekly trailers playlist
        if: always()
        run: |
          if [ -f youtube_credentials/token.pickle ]; then
            pip install google-api-python-client google-auth-oauthlib google-auth-httplib2
            python3 youtube_playlist_manager.py weekly || echo "YouTube playlist update failed, continuing..."
          else
            echo "Skipping YouTube playlist - no credentials"
          fi
        continue-on-error: true

      - name: Extract discovery metrics
        if: always()
        run: |
          if [ -f ".local_run.log" ]; then
            echo "📊 Discovery Metrics (Provider Availability):"
            echo "===================="

            # Extract standardized discovery metrics line
            discovery_line=$(grep -E "Polled [0-9]+ movies, [0-9]+ changes detected" .local_run.log | tail -n 1 || echo "")

            if [ -n "$discovery_line" ]; then
              # Parse the line to extract numbers
              polled=$(echo "$discovery_line" | grep -oE "Polled [0-9]+" | grep -oE "[0-9]+")
              transitions=$(echo "$discovery_line" | grep -oE ", [0-9]+ changes detected" | grep -oE "[0-9]+")

              echo "Polled Movies: $polled"
              echo "Changes Detected: $transitions"

              # Add to GitHub step summary
              {
                echo "## 🔍 Discovery Metrics (Provider Availability)"
                echo ""
                echo "| Metric | Count |"
                echo "|--------|-------|"
                echo "| 🎬 Polled Movies | $polled |"
                echo "| ⚡ Changes Detected | $transitions |"
                echo ""
              } >> $GITHUB_STEP_SUMMARY

              # Export to GitHub environment for later steps
              echo "DISCOVERY_POLLED=$polled" >> $GITHUB_ENV
              echo "DISCOVERY_TRANSITIONS=$transitions" >> $GITHUB_ENV
            else
              echo "⚠️ No standardized discovery metrics found in log"
              echo "DISCOVERY_POLLED=0" >> $GITHUB_ENV
              echo "DISCOVERY_TRANSITIONS=0" >> $GITHUB_ENV
            fi
          else
            echo "⚠️ No log file found (.local_run.log)"
            echo "DISCOVERY_POLLED=0" >> $GITHUB_ENV
            echo "DISCOVERY_TRANSITIONS=0" >> $GITHUB_ENV
          fi

      - name: Verify new arrivals in data.json
        if: always()
        run: |
          if [ -f "data.json" ]; then
            today=$(date -u +%Y-%m-%d)

            # Count new arrivals for today (adjust field name if needed)
            new_arrivals=$(jq -r --arg d "$today" '[.movies[] | select(.digital_date == $d)] | length' data.json 2>/dev/null || echo "0")

            echo "📊 New Arrivals Verification:"
            echo "============================"
            echo "Date: $today"
            echo "New Arrivals: $new_arrivals"

            # Add to GitHub step summary
            {
              echo "## 📅 New Arrivals"
              echo ""
              echo "| Metric | Value |"
              echo "|--------|-------|"
              echo "| 📅 Date | $today |"
              echo "| 🆕 New Arrivals | $new_arrivals |"
              echo ""
            } >> $GITHUB_STEP_SUMMARY

            # Export for commit message
            echo "NEW_ARRIVALS=$new_arrivals" >> $GITHUB_ENV
          else
            echo "⚠️ data.json not found - cannot verify new arrivals"
            echo "NEW_ARRIVALS=0" >> $GITHUB_ENV
          fi

      - name: Extract per-phase timing summary
        if: always()
        run: |
          if [ -f ".local_run.log" ]; then
            echo "📊 Per-Phase Performance Summary:"
            echo "================================"
            # Extract phase timing lines from the log
            grep -E "⏱️.*Phase Timings:|✅.*:.*s$|❌.*:.*s$" .local_run.log || echo "No phase timing data found"
            echo ""
            # Extract total duration
            grep -E "⏱️.*Total Duration:" .local_run.log || echo "No total duration found"
          else
            echo "⚠️ No log file found (.local_run.log)"
          fi

      - name: Check for Google search URLs in data.json
        run: |
          if [ -f "data.json" ]; then
            search_urls=$(jq -r '.movies[] | select(.watch_links != null) | .watch_links[] | select(.link != null) | .link' data.json 2>/dev/null | grep -c 'google.com/search' || echo "0")
            if [ "$search_urls" -gt 0 ]; then
              echo "⚠️ WARNING: Found $search_urls Google search URLs in data.json"
            else
              echo "✅ No Google search URLs found in data.json"
            fi
          else
            echo "⚠️ data.json not found, skipping search URL check"
          fi

      - name: Extract discovery summary
        if: always()
        run: |
          echo "📊 Discovery Summary (Provider Availability):"
          echo "===================="

          # Output to both console and GitHub summary
          {
            echo "## 📊 Discovery Summary (Provider Availability)"
            echo ""
            echo "| Metric | Count |"
            echo "|--------|-------|"
            echo "| 🔍 Polled Movies | ${DISCOVERY_POLLED:-0} |"
            echo "| ⚡ Changes Detected | ${DISCOVERY_TRANSITIONS:-0} |"
            echo "| 🆕 New Arrivals | ${NEW_ARRIVALS:-0} |"
            echo ""
          } | tee -a $GITHUB_STEP_SUMMARY

      - name: Handle pipeline failure
        if: failure()
        run: |
          echo "🔍 Discovery pipeline failed - checking for issues..."
          echo "WORKFLOW_URL=${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> $GITHUB_ENV
          echo "FAILURE_TIME=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_ENV


      - name: Check for changes
        id: changes
        if: always()
        run: |
          # Check for changes in data.json, movie_tracking.json, and all metrics files
          # Note: metrics/run_diagnostics.json tracks orchestrator health over time
          if ! git diff --quiet --exit-code -- data.json movie_tracking.json metrics/; then
            echo "changes=true" >> $GITHUB_OUTPUT
            echo "Changes detected in repository"
          else
            echo "changes=false" >> $GITHUB_OUTPUT
            echo "No changes detected"
          fi

      - name: Archive metrics artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: discovery-metrics-${{ github.run_id }}
          path: |
            metrics/intake_run.json
            metrics/discovery_run.json
            metrics/newly_available.json
            metrics/*.jsonl
            .local_run.log
          retention-days: 30

      - name: Archive data artifacts (recovery backup)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-backup-${{ github.run_id }}
          path: |
            movie_tracking.json
            data.json
          retention-days: 7

      # Hand the CI-generated curation caches back to the local Mac.
      # Stable artifact name so local_daily.sh can fetch the latest run's copy.
      # Never includes approved_capsules.json (the curated bank stays local).
      - name: Upload curation caches (pull quotes + capsules)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: curation-caches
          path: |
            cache/pull_quotes_combined.json
            cache/capsule_cache.json
          retention-days: 7
          if-no-files-found: warn

      - name: Commit changes
        if: always() && steps.changes.outputs.changes == 'true'
        run: |
          # Build commit message with discovery summary
          commit_msg="Daily NRW Update - $(date +%Y-%m-%d)"

          # Add discovery metrics if we have them
          if [ "${DISCOVERY_POLLED:-0}" != "0" ] || [ "${DISCOVERY_TRANSITIONS:-0}" != "0" ]; then
            commit_msg="$commit_msg"$'\n\n'"Discovery: polled=${DISCOVERY_POLLED:-0}, transitions=${DISCOVERY_TRANSITIONS:-0}"
          fi

          # Add new arrivals count
          if [ "${NEW_ARRIVALS:-0}" != "0" ]; then
            commit_msg="$commit_msg, new_arrivals=${NEW_ARRIVALS:-0}"
          fi

          # Stage generated files (data.json, archive, movie_tracking.json, metrics, wikipedia cache,
          # and the reissue candidate queue written by intake Pass D — otherwise daily-collected
          # reissues are discarded when the CI job ends and never reach the next /curate)
          echo "📦 Staging data.json, data_archive.json, movie_tracking.json, metrics, wikipedia cache, reissue queue"
          git add data.json data_archive.json movie_tracking.json metrics/ cache/wikipedia_cache.json admin/reissue_candidates.json

          # Only commit if there are staged changes and set flag accordingly
          if git diff --cached --quiet; then
            echo "No changes to commit"
            echo "COMMIT_MADE=false" >> $GITHUB_ENV
          else
            if git commit -m "$commit_msg"; then
              echo "COMMIT_MADE=true" >> $GITHUB_ENV
            else
              echo "Commit failed"
              echo "COMMIT_MADE=false" >> $GITHUB_ENV
            fi
          fi

      - name: Push changes to main
        if: always() && env.COMMIT_MADE == 'true'
        run: |
          # Pull latest changes first to avoid conflicts (rebase our commit on top)
          git pull --rebase origin main || {
            echo "⚠️ Rebase failed, attempting merge strategy..."
            git rebase --abort 2>/dev/null || true
            git pull origin main --no-rebase
          }
          git push origin main


      - name: Get pipeline status for failure report
        if: failure()
        id: pipeline-status
        run: |
          echo "pipeline_status=NRW Daily Update pipeline failed" >> $GITHUB_OUTPUT
          echo "failure_time=${FAILURE_TIME:-$(date -u +%Y-%m-%dT%H:%M:%SZ)}" >> $GITHUB_OUTPUT

      - name: Create NRW Daily Update failure issue
        if: failure() && env.NRW_NOTIFICATIONS == 'true'
        uses: actions/github-script@v7
        env:
          WORKFLOW_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          PIPELINE_STATUS: ${{ steps.pipeline-status.outputs.pipeline_status }}
          FAILURE_TIME: ${{ steps.pipeline-status.outputs.failure_time }}
        with:
          script: |
            const pipelineStatus = process.env.PIPELINE_STATUS || 'Unknown';
            const failureTime = process.env.FAILURE_TIME || new Date().toISOString();
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `NRW Daily Update Failed - ${new Date().toISOString().split('T')[0]}`,
              body: `## NRW Daily Update Pipeline Failure

            **Workflow Run:** ${process.env.WORKFLOW_URL}
            **Failure Time:** ${failureTime}
            **Branch:** ${context.ref}
            **Pipeline Status:** ${pipelineStatus}

            ### Debugging Steps:
            1. Check the workflow logs at the URL above
            2. Review \`daily_orchestrator.py\` output for specific errors
            3. Verify API keys are properly configured in repository secrets
            4. Check data quality validation errors
            5. Review intake (\`--intake\`), provider-availability discovery (\`--discover\`), and enrichment phases

            ### Manual Recovery:
            If needed, run the NRW Daily Update pipeline manually:
            \`\`\`bash
            python3 daily_orchestrator.py
            \`\`\`

            ### Impact:
            - NRW Daily Update pipeline failed
            - Site content may not be updated

            This issue was created automatically by the NRW Daily Update workflow.`,
              labels: ['automation', 'nrw-daily-update-failure', 'bug']
            })
