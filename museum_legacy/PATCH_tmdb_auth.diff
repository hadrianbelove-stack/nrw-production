--- tmdb_auth.py
+++ tmdb_auth.py
@@ -7,6 +7,15 @@ def _load_cfg():
     except FileNotFoundError:
         return {}
 
+def _first_nonempty(*vals):
+    for v in vals:
+        if isinstance(v, str):
+            v = v.strip()
+        if v:
+            return v
+    return None
+
 def resolve_tmdb_auth():
     """
     Order:
@@ -16,8 +25,12 @@ def resolve_tmdb_auth():
       4) config.yaml: tmdb.api_key | tmdb_api_key | api_key (fallbacks)
     Returns: (headers_dict, params_dict)
     """
     cfg = _load_cfg()
     tmdb = (cfg.get('tmdb') or {})
-    bearer = os.getenv('TMDB_BEARER') or tmdb.get('bearer')
-    api_key = os.getenv('TMDB_API_KEY') or tmdb.get('api_key')
+    bearer = _first_nonempty(os.getenv('TMDB_BEARER'), tmdb.get('bearer'))
+    api_key = _first_nonempty(os.getenv('TMDB_API_KEY'),
+                              tmdb.get('api_key'),
+                              tmdb.get('tmdb_api_key'),
+                              cfg.get('tmdb_api_key'),
+                              cfg.get('api_key'))
     if bearer:
         return {"Authorization": f"Bearer {bearer}"}, {}
     if api_key: